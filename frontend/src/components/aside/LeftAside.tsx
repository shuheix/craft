import { Box } from "@chakra-ui/react";
import React, { VFC } from "react";

const Aside: VFC = () => {
  return (
    <Box bgColor="green.100" w={280}>
      左再度です
    </Box>
  );
};

export default Aside;
